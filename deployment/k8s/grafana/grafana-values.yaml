serviceAccount:
  create: false
  name: grafana-ksa

rbac:
  create: false
  
service:
  type: LoadBalancer
  loadBalancerSourceRanges:
    - "0.0.0.0/0"
adminUser: admin

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 250m
    memory: 512Mi

persistence:
  enabled: true
  size: 1Gi

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      # Section A + B
      - name: Google Cloud Monitoring
        uid: google-cloud-monitoring
        type: stackdriver
        access: proxy
        isDefault: true
        editable: true
        jsonData:
          authenticationType: gce
          defaultProject: project-81c35834-dcf8-4001-9b2

      # Section C + D
      - name: BigQuery
        type: grafana-bigquery-datasource
        access: proxy
        jsonData:
          authenticationType: gce
          defaultProject: project-81c35834-dcf8-4001-9b2
          processingLocation: asia-southeast1

plugins:
  - grafana-bigquery-datasource